Create a custom exception InvalidDepositException if the user deposits a negative amount. 

Add an AccountNotFoundException for accessing invalid account numbers. 

Handle multiple custom exceptions in the same program using multiple catch blocks.