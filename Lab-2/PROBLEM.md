Create a package conataining class named Store having some attributes and methods,

Inside store package create two more child packages classes for Grocery Store and Music Store inheriting from Store package class.
the child packages classes will also have their own attributes and methods as well.

then create a Driver class outside the packages for the main function in which we will create Objects of those packages classes and other stuff on them. 


